# Production environment configuration for Vite
# This file is used during `npm run build` for production builds

# Empty API URL means same-origin (requests go through nginx proxy)
# This is critical for production deployment where frontend and API
# are served from the same domain via nginx reverse proxy
VITE_API_URL=
